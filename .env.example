# Bright Data API key — only needed for headless runs.
# Interactive use: `npx -p @brightdata/cli bdata login` stores credentials locally instead.
BRIGHTDATA_API_KEY=

# Where the SQLite database lives (default: ./data/mycelium.sqlite)
MYCELIUM_DB=
